#
# Do not modify the Config while the Server
# is running otherwise bad things might happen!
#
# Name: QuickSell
# Authors: mrCookieSlime, Shay Punter
# Exclusive version for LookCraft.
# Maintainer: Sliide_
#

# Do NOT change this!
config-version: 1

options:
  sell-gui-rows: 3 # Specify how many rows the Sell-GUI has
  locked-item: BARRIER # The Item which will be used if the Shop is unaccessable.
  sign-prefix: '&7[&eSell&7]' # The Prefix which Sell Signs will have. Changing it to e.g. "Sell Me" requires you to make the first line "Sell Me" as well in order to create a Sell Sign.
  sellall-sign-prefix: '&a&oSell All' # Same rules apply for the Sell all Signs
  enable-menu-line: true # Enabling the Menu line will add 3 nifty Buttons to the GUI: Sell, Estimate and Cancel

shop:
  enable-hierarchy: false
  enable-logging: true

boosters:
  extension-mode: true # Enabling this means if you activate a 2x Booster while another 2x Booster is already active it will increase the active Booster's time instead of stacking it
  refresh-every: 10 # The Interval in Seconds when it checks whether a Booster wore off

commands-on-sell: # Commands which will be executed on selling something. Supports the Variables {PLAYER} and {MONEY}
  - say %player% just sold something and got $%money%

sound:
  enabled: true # Whether it should play a Sound when you sell something
  sound: ENTITY_EXPERIENCE_ORB_PICKUP # The Sound which will be played

